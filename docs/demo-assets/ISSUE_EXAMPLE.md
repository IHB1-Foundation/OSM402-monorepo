# 이슈 예시(복붙)

## 제목

`Bug: add()가 잘못된 값을 반환해요`

## 본문

```
### 문제
`src/calc.js`의 `add(a, b)`가 더하기가 아니라 빼기를 하고 있어요.

### 기대 동작
add(2, 3) === 5

### 재현
1) `npm test` 실행
2) `add()` 관련 테스트가 실패함

### 완료 조건(AC)
- `add()`가 정상적으로 동작
- `npm test` 통과
```

## 라벨(중요)

- `bounty:$0.1`

> 라벨을 붙이는 순간 OSM402가 “Bounty Detected” 코멘트를 남기고, 에이전트가 이 이슈를 펀딩할 준비를 합니다.
