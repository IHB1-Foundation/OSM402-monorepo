# =========================
# OSM402 local demo (.env)
# Target: BITE V2 Sandbox 2 + real USDC (0.1 USDC demo)
# =========================

# Server
PORT=3000
DATABASE_URL="file:./dev.db"

# Real-chain demo mode
X402_MOCK_MODE=false
ESCROW_MOCK_MODE=false

# GitHub webhook + server auth secrets
# Change both values before public demo.
GITHUB_WEBHOOK_SECRET=replace-with-strong-webhook-secret
OSM402_ACTION_SHARED_SECRET=replace-with-strong-action-secret

# GitHub auth (recommended: GitHub App)
GITHUB_APP_ID=
GITHUB_PRIVATE_KEY_PEM=
# Optional fallback (use either GitHub App or PAT)
GITHUB_TOKEN=

# Fixed demo chain: BITE V2 Sandbox 2
CHAIN_NAME=bite-v2-sandbox-2
CHAIN_ID=103698795
RPC_URL=https://base-sepolia-testnet.skalenodes.com/v1/bite-v2-sandbox-2
EXPLORER_URL=https://base-sepolia-testnet-explorer.skalenodes.com:10032
BITE_V2_SANDBOX_2_RPC_URL=https://base-sepolia-testnet.skalenodes.com/v1/bite-v2-sandbox-2

# Fill after deploying contracts
ESCROW_FACTORY_ADDRESS=

# Asset: existing USDC on BITE V2 Sandbox 2 (must use this)
ASSET_SYMBOL=USDC
ASSET_DECIMALS=6
ASSET_ADDRESS=0xc4083B1E81ceb461Ccef3FDa8A9F24F0d764B6D8
USDC_ADDRESS=0xc4083B1E81ceb461Ccef3FDa8A9F24F0d764B6D8

# Onchain keys (fill with your own test keys)
DEPLOYER_PRIVATE_KEY=
OSM402_MAINTAINER_PRIVATE_KEY=
OSM402_AGENT_PRIVATE_KEY=
X402_PAYER_PRIVATE_KEY=
OSM402_MAINTAINER_ADDRESS=
OSM402_AGENT_ADDRESS=

# Wallet mapping plan A (recommended for demo speed, same wallet OK):
# DEPLOYER_PRIVATE_KEY          = <same key>
# OSM402_MAINTAINER_PRIVATE_KEY = <same key>
# OSM402_AGENT_PRIVATE_KEY      = <same key>
# X402_PAYER_PRIVATE_KEY        = <same key>
#
# Wallet mapping plan B (split wallets):
# DEPLOYER_PRIVATE_KEY          = <deployer key>
# OSM402_MAINTAINER_PRIVATE_KEY = <maintainer key>
# OSM402_AGENT_PRIVATE_KEY      = <agent key>
# X402_PAYER_PRIVATE_KEY        = <payer key>
#
# If maintainer/agent key != deployer key, set both address envs explicitly.
# If all keys are same as deployer, both address envs can stay empty.

# Local URLs
OSM402_API_URL=http://localhost:3000
OSM402_BASE_URL=http://localhost:3000

# Demo defaults (low-cost)
DEMO_REPO=owner/repo
DEMO_ISSUE=1
DEMO_PR=1
DEMO_BOUNTY=0.1
DEMO_ADDRESS=0x1111111111111111111111111111111111111111

# Optional AI review
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
