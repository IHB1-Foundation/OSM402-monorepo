{
  "name": "@gitpay/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'build not configured yet'",
    "dev": "echo 'dev not configured yet'",
    "test": "echo 'test not configured yet'"
  }
}
